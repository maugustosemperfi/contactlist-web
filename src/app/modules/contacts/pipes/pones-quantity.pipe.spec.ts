import { PonesQuantityPipe } from './pones-quantity.pipe';

describe('PonesQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new PonesQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
